#include "main.h"
#include <stdio.h>

/**
* mul -multiples two integers
* @a: - first integer
* @b: - seconf integer
* Return: always 0
*/

int mul(int a, int b)
{
int result = a * b;

return (result);
}
